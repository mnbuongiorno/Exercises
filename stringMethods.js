let string = "Test String";
let numbers = 123;

// let string2 = "String 2";

// result = string.concat(string2); concat one or more strings and returns a new one
// result = string.startsWith(string2); if one string starts with another string, return true, else return false
// result = string.endsWith(string2); if one string ends with another string, return true, else return false
// result = string.includes(string2); search strings into another strings, return boolean
// result = string.indexOf("String"); search index and return index value, else return -1
// result = string.lastIndexOf("String"); return last index char of the string, else return -1

// result = string.padStart(6,"T"); fills the string with chars at the start 
// result = string.padEnd(3,"S"); fills the string with chars at end
// result = string.repeat(2); return repeated string equal to number 

// result = string.split(" ") split the string and returns array
// result = string.substring(0,3);  create new string (0 init, 3 fin)
// result = string.toLowerCase(); 
// result = string.toUpperCase();
// result = numbers.toString();

// result = string.trim(); delete empty spaces
// result = string.trimEnd(); delete empty spaces at end
// result = string.trimStart(); delete empty spaces at start

document.write(result);
